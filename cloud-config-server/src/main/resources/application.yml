server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devwdougherty/config-server-dailycodebuffer-workshop
          clone-on-start: true